
<div class="row">
	<div class="col-xs-3">

		<h2>User info</h2>
		<%= render 'users/profile', user:@user %>

		<h2>New book</h2>
		<%= render 'books/newform', book:@new_book %>

	</div>

	<div class="col-xs-9">
		<h2>Book detail</h2>

		<table class="table">
			<%= render 'bookinfo', book: @book %>
		</table>

		<%= render 'book_comments/newform'%>

		<%= render 'book_comments/comment', {book: @book, book_comments: @book_comments} %>

	</div>
</div>

